const bundlePath = 'src/components/menu/nls/menu';
const locales: string[] = [];

const messages = {
	title: 'MHC',
	dashboard: 'Dashboard',
	logout: 'Log Out'
};

export default { bundlePath, locales, messages };
